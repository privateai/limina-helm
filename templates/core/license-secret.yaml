{{- if not .Values.externalsecrets.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.core.deployment.license.name }}
  {{- with .Values.core.deployment.license.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "limina.labels" . | nindent 4 }}
data:
  license.json: |-
{{- .Values.core.deployment.license.data | b64enc | nindent 4 }}
{{- end }}
