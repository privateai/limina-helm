Successfully installed {{ .Release.Name }} version {{ .Chart.AppVersion }}

NOTE: It can take a few minutes to download and start the container.
To get the status of the container, run the following
      kubectl --namespace {{ .Release.Namespace }} get pods

{{- if .Values.core.ingress.enabled }}
To test the ingress connectivity, run the following
      curl {{ if .Values.core.ingress.selfsigned }}-k {{ end }}https://{{ .Values.core.ingress.host }}/
{{- else if eq "LoadBalancer" .Values.core.service.type }}

NOTE: It may take a few minutes for the LoadBalancer IP to be available.
To get the status of the load balancer, run the following
      kubectl --namespace {{ .Release.Namespace }} get service {{ include "limina.fullname" . }}

To test the service connectivity, run the following
  {{- if eq "AWS" .Values.cloudProvider }}
      export SERVICE_IP=$(kubectl get service --namespace {{ .Release.Namespace }} {{ include "limina.fullname" . }} --template "{{"{{ range .status.loadBalancer.ingress }}{{ .hostname }}{{ end }}"}}")
  {{- else if eq "Azure" .Values.cloudProvder }}
      export SERVICE_IP=$(kubectl get service --namespace {{ .Release.Namespace }} {{ include "limina.fullname" . }} --template "{{"{{ range .status.loadBalancer.ingress }}{{ .ip }}{{ end }}"}}")
  {{- end }}
      curl http://$SERVICE_IP:{{ .Values.core.service.port }}
{{- end }}
