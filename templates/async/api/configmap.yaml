{{- if and .Values.shared.async.enabled .Values.api.configmap.data }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "limina.fullname" . }}-api
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "limina.labels" . | nindent 4 }}
data:
{{- range .Values.api.configmap.data }}
  {{ .name }}: {{ .value | quote }}
{{- end }}
{{- end }}
