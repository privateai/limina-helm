{{- if and (not .Values.externalSecrets.enabled) .Values.shared.async.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "limina.fullname" . }}-custodian
  namespace: {{ .Release.Namespace }}
  {{- with .Values.custodian.secrets.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "limina.labels" . | nindent 4 }}
data:
{{- range .Values.custodian.secrets.data }}
  {{ .name }}: {{ .value | b64enc }}
{{- end }}
{{- end }}
