{{- if and .Values.shared.async.enabled .Values.custodian.configmap.data }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "limina.fullname" . }}-custodian
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "limina.labels" . | nindent 4 }}
data:
{{- range .Values.custodian.configmap.data }}
  {{ .name }}: {{ .value | quote }}
{{- end }}
{{- end }}
