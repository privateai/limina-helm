apiVersion: batch/v1
kind: Job
metadata:
  name: '{{ include "limina.fullname" . }}-test-connection'
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "limina.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  ttlSecondsAfterFinished: 0
  template:
    spec:
      containers:
        - name: wget
          image: busybox
          command: ['wget']
          args: ['{{ include "limina.fullname" . }}:{{ .Values.service.port }}']
      restartPolicy: Never
